---
# Collections Ansible optionnelles
# Installation: ansible-galaxy collection install -r requirements.yml

collections:
  - name: community.docker
    version: ">=3.0.0"
  - name: community.general
    version: ">=7.0.0"
  - name: kubernetes.core
    version: ">=2.4.0"

